# Current Feature

## Status

Not Started

## Goals

## Notes

## History

- Initial Next.js and Tailwind setup
- Mock data added for dashboard UI (`src/lib/mock-data.ts`)
- Dashboard UI Phase 1 — Completed
- Dashboard UI Phase 2 — Completed
- Dashboard UI Phase 3 — Completed
- Prisma + Neon PostgreSQL setup — Completed
- Database seed (demo user + system types + 5 collections / 18 items) — Completed
- Dashboard collections wired to Neon DB + sidebar polish (uppercase TYPES/COLLECTIONS, chevrons, separator) — Completed
- Dashboard items (pinned + recent) wired to Neon DB with empty-state for pinned — Completed
- Sidebar wired to Neon DB (item types with counts, collections favorites/recent with dominant-type circles, View all link) + COLLECTIONS group collapse toggle — Completed
- PRO badge added to `file` and `image` types in sidebar (subtle outline shadcn Badge, centered between label and count, hidden in collapsed mode) — Completed
- Code audit quick wins — N+1 fix in `fetchCollectionsWithMeta` (Prisma `_count` + bounded `itemCollection` findMany), deleted dead `mock-data.ts`, irregular-plurals lookup in sidebar `pluralize`, batched seed with `createManyAndReturn`, dropped password-hash select from `test-db.ts` — Completed
- Auth Phase 1 — NextAuth v5 + GitHub OAuth with split config (edge-compatible `auth.config.ts` + full `auth.ts` with Prisma adapter), JWT session, and `src/proxy.ts` gating `/dashboard` — Completed
- Auth Phase 2 — Credentials provider (email/password) added via split-config override in `auth.ts` with bcryptjs validation, plus `POST /api/auth/register` (Node runtime) for new-user signup — Completed
- Auth Phase 3 — Custom `/sign-in` and `/register` pages replacing NextAuth defaults, reusable `UserAvatar` (image-or-initials), sidebar footer dropdown (Profile + Sign out), `/profile` placeholder, sonner toast on post-registration redirect, dashboard wired to authenticated session instead of demo user — Completed
- Email verification on register — Resend SDK integration sending single-use 24h SHA-256-hashed tokens via existing `VerificationToken` model, `GET /api/auth/verify` + `POST /api/auth/resend-verification` (leak-resistant), `/verify-email` page with sent/verified/expired/invalid states, unverified credentials sign-in blocked via `EmailNotVerifiedError` with inline resend button, GitHub OAuth users auto-verified via `events.linkAccount`, plus `scripts/reset-users.ts` testing helper — Completed
- Email verification toggle — `EMAIL_VERIFICATION_ENABLED` env var (default on) centralized in `src/lib/email.ts`; when set to `"false"`, register auto-sets `emailVerified` and skips Resend, credentials sign-in stops throwing `EmailNotVerifiedError`, and `/api/auth/resend-verification` short-circuits to the existing leak-resistant no-op — Completed
- Forgot password — `/forgot-password` and `/reset-password` pages with sent/invalid/expired/success states, `POST /api/auth/forgot-password` (leak-resistant, skips OAuth-only users) + `POST /api/auth/reset-password` issuing single-use 1h SHA-256-hashed tokens via `VerificationToken` with `password-reset:<email>` identifier prefix, `consumeVerificationToken` hardened to reject cross-purpose tokens, `sendPasswordResetEmail()` mirroring the verification-email template, "Forgot password?" link added to `/sign-in`, independent of `EMAIL_VERIFICATION_ENABLED` — Completed
- Sign-in/register UI redesign — centered `max-w-md` rounded-2xl auth card with subtle border, dark elevated bg, and ambient radial-gradient backdrop; reordered sign-in flow (Email → Password w/ inline "Forgot password?" → primary CTA → "OR CONTINUE WITH" divider → GitHub → Register link); new reusable `EmailField`/`PasswordField`/`IconField` with leading Mail/Lock/User icons + Eye/EyeOff visibility toggle; `LoaderCircle` spinner inside CTAs replaces label-swap; reserved-height sign-in/register/forgot/reset forms now toast on 429 alongside their existing inline error (resend-verification button already toasts) — Completed
- Profile page — moved `/dashboard` and `/profile` into a shared `(dashboard)` route group so profile renders inside the sidebar/topbar shell; rebuilt `/profile` with two cards (Account Information: avatar + name/email/member-since + Change Password / Delete Account actions; Usage Statistics: total items + collections + per-system-type breakdown grid); `getUserProfileById` helper exposes `hasPassword` without leaking the hash; `POST /api/account/change-password` bcrypt-validates current password and rejects identical reuse; `deleteAccountAction` server action cascades the user delete via Prisma and signs out to `/`; reusable base-ui `Dialog` primitive (matches existing Sheet pattern) backs the confirmation dialogs (`KeyRound` / `Trash2` triggers, type-`DELETE`-to-confirm gate) — Completed
- Rate limiting for auth — reusable `src/lib/rate-limit.ts` on Upstash Redis + `@upstash/ratelimit` sliding-window (lazy-initialized client, per-name limiter cache, fail-open on missing env or thrown errors); wired into `/api/auth/register` (3/1h, IP), `/api/auth/forgot-password` (3/1h, IP), `/api/auth/reset-password` (5/15m, IP), `/api/auth/resend-verification` (3/15m, IP+email, applied after email validation), and login via `credentialsSignInAction` (5/15m, IP+email, using `await headers()` instead of intercepting NextAuth's callback route); 429 responses include JSON error + `Retry-After`; IP extracted from `x-forwarded-for` with `x-real-ip` fallback; sign-in/register/forgot/reset forms now toast on 429 alongside their existing inline error (resend-verification button already toasts) — Completed
- Items list view — dynamic `/items/[type]` route inside `(dashboard)` shell rendering type-filtered items in a `grid-cols-1 md:grid-cols-2` grid of `ItemCard`s with left border colored by `itemType.color`; `src/lib/system-types.ts` maps plural slugs ↔ singular `ItemType.name` for the 7 system types (snippets/prompts/commands/notes/files/images/links); `getSystemItemTypeByName` + `getItemsForUserByTypeId` added to `src/lib/db/items.ts`; page is `auth()`-gated with redirect-to-sign-in, `notFound()` on unknown slugs, header (type icon + capitalized label + item count), empty state on zero items, and `generateMetadata` for per-type page titles — Completed
- Vitest setup — Vitest 4 with Node env, native tsconfig paths (`@/* → src/*`), and a `vitest.setup.ts` that stubs `"server-only"`; scope limited to server actions (`src/actions/**`) and utilities (`src/lib/**`) — components excluded; Prisma + NextAuth mocked via `vi.mock` so tests are pure and fast; sample tests cover `system-types`, `utils.cn`, pure `rate-limit` helpers (`extractIp`/`formatRetryAfter`/`rateLimitMessage`), and `deleteAccountAction` (demonstrating the mocking pattern for `@/auth` + `@/lib/prisma`); `npm test` (watch) + `npm run test:run` (one-shot) added; `ai-interaction.md` workflow updated to require tests in step 4 alongside the build, and `coding-standards.md` gained a Testing section — Completed
- Items list 3-column grid — `/items/[type]` page grid bumped from `grid-cols-1 md:grid-cols-2` to `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` so wider viewports see more items per row (mobile 1 col / tablet 2 col unchanged); bundled fix to `vitest.config.ts` adding `fileParallelism: false` to work around Vitest 4 cross-worker module-resolution cache poisoning caused by `next-auth/lib/env.js` failing to resolve `next/server` under Node ESM, which broke the whole suite even though every test file passed in isolation — Completed
- Item detail drawer — right-side shadcn Sheet drawer opens on `ItemCard` click across `/dashboard` and `/items/[type]`; new `ClickableItemCard` client wrapper turns the existing `ItemCard` into a `<button>` and owns drawer state so pages stay server components; card data continues to come from server components while the full detail (content, file/url, language, collections, createdAt) is fetched on click via new `auth()`-gated `GET /api/items/[id]` returning the new `ItemDetail` from `getItemDetailForUser` in `src/lib/db/items.ts` (scoped by `userId`, flattens tags + collections); drawer header shows type icon + title + Type/language badges, action bar has Favorite (yellow star when active), Pin, Copy (wired to `navigator.clipboard`), Edit, Delete (right-aligned trash) — Favorite/Pin/Edit/Delete stubbed per spec; body renders Description, Content (`<pre>` for TEXT, external link for URL, file row with `formatBytes` for FILE), Tags, Collections, and Created/Updated/Last used in a dl grid; skeleton + error states while the fetch is in flight; AbortController cancels in-flight requests on close; `formatDate` coerces ISO strings since the API serializes Dates to JSON; new unit tests in `src/lib/db/__tests__/items.test.ts` cover `getItemDetailForUser` (null on miss, full field mapping, user scoping) — route handler not unit-tested because importing it pulls next-auth's `next/server` resolution into Vitest and poisons the suite (the same issue the existing fileParallelism workaround can't fully escape) — Completed
- Item drawer edit mode — pencil Edit toggles the open drawer into inline edit mode, swapping the action bar to Save (primary, left) + Cancel and the body to a controlled form with Title (required), Description, Tags (comma-separated), and type-aware Content (snippet/prompt/command/note), Language (snippet/command), URL (link); new `updateItemAction` in `src/actions/items.ts` returns `{ success, data | error }`, validates with Zod 4 (trimmed required `title`, optional trimmed string fields, `new URL()`-checked optional `url`, deduped trimmed `tags`), checks `auth()` session, and delegates to new `updateItemForUser` in `src/lib/db/items.ts` which verifies ownership and runs `tagsOnItems.deleteMany` + `item.update` (with `tags.create` + `connectOrCreate`) inside a `$transaction`, returning the refreshed `ItemDetail` so the drawer skips a second fetch; client uses `useTransition` for pending state, toasts on success/error, calls `router.refresh()` to sync the underlying card lists, and disables Save when title is empty; manual `useCallback`/`useMemo` dropped throughout the drawer since `reactCompiler: true` is on in `next.config.ts`; `zod` added as a direct dependency; new Vitest tests in `src/actions/__tests__/items.test.ts` (7 cases: no session, empty id, empty title, invalid URL, not-found, normalization, generic-error) and three more in `src/lib/db/__tests__/items.test.ts` covering `updateItemForUser` (ownership miss, tag dedup + connect-or-create shape, refreshed-detail return) — Completed
- Item delete — trash icon in the drawer action bar now opens a base-ui `Dialog` confirmation ("Delete \"<title>\"?" + "cannot be undone" warning, dismiss locked while pending) that calls new `deleteItemAction` in `src/actions/items.ts`; action validates `auth()` session + non-empty id, delegates to new `deleteItemForUser` in `src/lib/db/items.ts` which uses ownership-scoped `prisma.item.deleteMany({ where: { id, userId } })` so wrong-owner and missing-id collapse to `count === 0` (no enumeration oracle) and Prisma's `onDelete: Cascade` cleans up `TagsOnItems` + `ItemCollection`; on success the drawer closes via `onOpenChange(false)`, toasts `"Item deleted"`, and calls `router.refresh()` so dashboard pinned/recent + items-by-type grids re-fetch; new Vitest cases in `src/actions/__tests__/items.test.ts` (no session, empty id, not-found, success, generic-error) and `src/lib/db/__tests__/items.test.ts` (scoping shape, deleted, no-match) — Completed
- Item create — new `NewItemDialog` client component wired to the top-bar "New Item" button: shadcn `Dialog` with a radio-style 5-type selector (snippet/prompt/command/note/link, colored Lucide icons) and type-aware fields (title required for all, content for snippet/prompt/command/note, language for snippet/command, URL required for link, comma-separated tags for all); new `createItemAction` in `src/actions/items.ts` returns `{ success, data | error }`, validates with Zod 4 (trimmed required title, optional trimmed strings, `new URL()`-checked URL, `superRefine` requiring URL for link, deduped trimmed tags), checks `auth()` session, then strips fields irrelevant to the chosen type before delegating to new `createItemForUser` in `src/lib/db/items.ts`; the db helper looks up the system `ItemType` by name (returns null when missing so the action surfaces "Item type not found"), derives `contentType` (`TEXT` for snippet/prompt/command/note, `URL` for link) from a per-type lookup table, dedupes tags inside the helper via `Array.from(new Set(...))` (matches the `updateItemForUser` pattern), runs `prisma.item.create` with `tags.connectOrCreate`, then re-reads via `getItemDetailForUser` so the action returns the refreshed `ItemDetail`; client uses `useTransition`, toasts on success/error, resets all state on close (locked while pending), and calls `router.refresh()` so sidebar counts + dashboard lists pick up the new row; new Vitest tests in `src/actions/__tests__/items.test.ts` (8 cases: no session, empty title, missing URL for link, invalid URL, normalization with type-irrelevant field stripping, link keeps URL drops content/language, snippet keeps language, type-not-found, generic-error) and `src/lib/db/__tests__/items.test.ts` (4 cases: type not found, snippet creates TEXT with deduped connect-or-create tags, link creates URL contentType, refreshed-detail return) — Completed
- Dashboard UI typography & spacing refresh — purely visual polish normalizing the typography scale across the dashboard against the sidebar baseline; dashboard section `h2`s bumped from `text-base` to `text-lg font-semibold` with section gap `gap-3 → gap-4`, "View all" + pinned/items-by-type empty-state helpers from `text-xs → text-sm`; `ItemCard` retitled to `text-[15px] leading-snug` (matches sidebar's `text-[15px]` menu items), description `text-xs → text-sm leading-snug`, badges `text-[10px] → text-[11px]` with gap `1 → 1.5`, date `text-xs → text-sm`, pin/star icons `size-3 → size-3.5`; item detail drawer reflows to `px-6 py-5` header (gap-4) + `px-6 py-6` body (gap-6), `SheetTitle` `text-base → text-lg`, header badges `text-[10px] → text-[11px]`, content `<pre>` `text-xs → text-sm` with `p-4 leading-relaxed`, tag/collection badges `text-[11px] → text-xs`, dl `gap-y-1.5 → gap-y-2`, file row + EmptyContent + Open link `text-xs → text-sm`, Field gap `1.5 → 2`, edit-mode content textarea `text-xs → text-sm`; `NewItemDialog` type selector buttons `text-[11px] → text-xs` (gap `1 → 1.5`, py `2 → 2.5`), Field/Type group gap `1.5 → 2`, content textarea `text-xs → text-sm`; top-bar search input `h-9 → h-10` with explicit `text-sm` + `pl-10` (icon shifted to `left-3.5`), ⌘K kbd `text-[10px] → text-[11px]`; no functionality/state/API/routing changes, all 53 Vitest tests + `next build` pass — Completed
- Code editor — new `CodeEditor` client component on `@monaco-editor/react` (`devstash-dark` theme defined in `beforeMount`, macOS dots + language label + copy button in the header, themed scrollbar slider, fluid height clamped 80–400px via `onDidContentSizeChange`, language alias map e.g. `ts → typescript`); item detail drawer view mode renders read-only `CodeEditor` (hidden cursor + no line highlight) for snippet/command, edit mode renders editable `CodeEditor` — prompt/note still use `<pre>` / `<Textarea>`; `NewItemDialog` Content field swaps to `CodeEditor` for snippet/command and now accepts `initialType?: CreateItemType` + `trigger?: React.ReactElement` props, with `baseType` seeded from `initialType` so reopening keeps the preselection; `/items/[type]` page header is now a `justify-between` flex with a `New <Singular>` button on the right for the 5 creatable types (snippet/prompt/command/note/link; `file`/`image` excluded) that opens the dialog pre-selected; drawer max-width bumped from `sm:max-w-lg` to `sm:max-w-[max(32rem,33vw)]` so it scales to ~1/3 of the viewport on large monitors while keeping the 32rem floor on laptops and `w-full` on mobile; no new server actions/utilities so no Vitest additions — all 53 tests + `next build` still pass — Completed
- Markdown editor — new `MarkdownEditor` client component on `react-markdown` + `remark-gfm` with a tabbed header (Write/Preview in edit mode, Preview-only in readonly), copy button matching `CodeEditor`'s header treatment, auto-grow `<textarea>` clamped 80–400px via a `useEffect` scrollHeight measure, dark `#1a1a1a` container + `#171717` header to match `CodeEditor` visually; new `.markdown-preview` styles in `globals.css` for h1–h6 (sized + underlined on h1/h2), paragraphs, GFM links (blue + hover underline), bold/italic, ordered/unordered/nested lists, blockquotes (left border + muted bg), inline code (subtle bg pill) and `<pre>` blocks (`#0f0f0f` bg, monospaced), `<hr>`, GFM tables (bordered cells + header bg + zebra rows), images, and GFM task-list checkboxes; reusable `.devstash-scroll` utility (in `@layer utilities`, also covering `.markdown-preview pre/table`) that themes the native scrollbar to match Monaco's `scrollbarSlider` colors — translucent zinc thumb at default/hover/active, pill-shaped via `border + background-clip: padding + background-clip: padding-box`, scrollbar-buttons hidden — with Firefox `scrollbar-width/scrollbar-color` fallbacks; `ItemDrawer` view mode renders readonly `MarkdownEditor` for note/prompt (replacing the `<pre>`), edit mode renders editable `MarkdownEditor` (replacing the `<Textarea>`), `NewItemDialog` content field renders `MarkdownEditor` for note/prompt — snippet/command unchanged; bundled hydration fix dropping `data-slot="dialog-trigger"` from `DialogTrigger` in `src/components/ui/dialog.tsx` because base-ui's render-prop slot merge resolved its collision with `Button`'s `data-slot="button"` differently between SSR and CSR (showed up as a real-tree-hydrated mismatch on the `/items/[type]` "New <Singular>" button); no new server actions/utilities so no Vitest additions — all 53 tests + `next build` still pass — Completed
- File & image upload (Cloudflare R2) — new `@aws-sdk/client-s3`-backed `src/lib/r2.ts` (lazy-init client targeted at `https://<account>.r2.cloudflarestorage.com` with `region: "auto"`, `publicUrlForKey`/`keyFromPublicUrl` helpers that strip and re-attach the trailing slash on `R2_PUBLIC_URL`, `uploadObjectToR2`/`deleteObjectFromR2`, and `getObjectFromR2` that normalizes the SDK's Node `Readable` body to a Web `ReadableStream` via `Readable.toWeb()` so the proxy can return it as a `Response` body); shared client/server validation in `src/lib/upload-constraints.ts` (images ≤5 MB / `.png|.jpg|.jpeg|.gif|.webp|.svg`, files ≤10 MB / `.pdf|.txt|.md|.json|.yaml|.yml|.xml|.csv|.toml|.ini`, MIME allowlist that tolerates `""`/`application/octet-stream`/`text/plain`-for-`.ini` since browsers report these inconsistently); new `POST /api/upload` (Node runtime, auth-gated, `formData()` parse, server-side re-validation, post-buffer size re-check, `uploads/<userId>/<crypto.randomUUID()><ext>` keys, returns `{ fileUrl, fileName, fileSize, mimeType }`); new `GET /api/files/[id]` download proxy that resolves the item via `getItemDetailForUser` (ownership-scoped), extracts the R2 key via `keyFromPublicUrl`, streams the body back with `Content-Disposition: attachment` + RFC-5987 `filename*=UTF-8''…` encoding (with ASCII fallback for legacy clients) and `Cache-Control: private, no-store`; new `FileUpload` client component with drag/drop drop zone (role=button, Enter/Space + drag events, focus-visible ring), hidden `<input type="file">`, `XMLHttpRequest` upload with `upload.progress` → progress bar, `URL.createObjectURL` image preview revoked on remove + unmount, in-flight `xhr.abort()` on unmount and remove, error toasts via inline `role="alert"`; `NewItemDialog` gains `file`/`image` in the type radio (7-column grid), renders `FileUpload` for those types via `TYPES_WITH_UPLOAD`, blocks Create until upload finishes (`uploadMissing` gate), clears `uploaded` when switching to a non-upload type, and the `Field` component now accepts an optional `htmlFor` so the upload field doesn't ship a dangling label; `/items/[type]` page adds `file`/`image` to `CREATABLE_TYPES` so "New File" / "New Image" buttons appear on those pages; `ItemDrawer` renders an inline `<img>` preview for `image` items (with the file-meta row below) and replaces the old "Open" link with a Download link (`href=/api/files/<id>`, `download="<filename>"`); `createItemForUser`/`createItemInput` extended with `fileUrl`/`fileName`/`fileSize` and `contentType: "FILE"` derived from the existing per-type table; `deleteItemForUser` now looks up `fileUrl` first then calls `deleteObjectFromR2` after the DB row is gone (try/catch logs but doesn't fail the user-facing delete on R2 errors, so an outage can't strand the item); `createItemAction` Zod schema (Zod 4) accepts `fileUrl`/`fileName`/`fileSize`, requires them for `file`/`image` via `superRefine`, and adds a server-side ownership check that extracts the R2 key from the client-supplied `fileUrl` and verifies it starts with `uploads/<sessionUserId>/` — blocks cross-user URL injection where User B could otherwise create an item pointing at User A's R2 object and then download it via the proxy; new Vitest coverage — `src/lib/__tests__/upload-constraints.test.ts` (9 cases incl. octet-stream tolerance and `.ini` text/plain branch), `src/lib/__tests__/r2.test.ts` (6 cases incl. trailing-slash handling and prefix-confusion guard against `files.example.com.evil`), action tests gained file-required, negative-fileSize, file/image normalization, cross-user-key rejection, non-bucket-URL rejection, and prefix-confusion (`uploads/user_1foo/…`) rejection cases, db tests gained file/image create path + four `deleteItemForUser` paths (scoping, no-file skip, R2 cleanup, R2-throw-still-succeeds); bundled `vitest.config.ts` fix adding `isolate: true` alongside the existing `fileParallelism: false` because the next-auth `next/server` resolution poison was still leaking across files within a single worker — 80 tests + `next build` pass — Completed
- Image gallery view — `/items/images` swaps the standard `ItemCard` for a new image-thumbnail card; new `ImageThumbnailCard` (server) renders an `aspect-video` `object-cover` `<img>` from `item.fileUrl` inside an `overflow-hidden` frame with a 5% / 300ms `group-hover:scale-105` zoom (Tailwind v4 emits this via the `scale:` CSS property under `@media (hover: hover)`), a title strip below the thumbnail, a pinned/favorite badge overlay (top-right, backdrop-blur), and an `ImageIcon` fallback when `fileUrl` is null; new `ClickableImageCard` client wrapper mirrors `ClickableItemCard` and keeps the existing `ItemDrawer` open-on-click behavior unchanged; `/items/[type]/page.tsx` branches on `typeName === "image"` to render a `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` gallery — other types keep the existing `md:grid-cols-2 lg:grid-cols-3 gap-3` ItemCard grid; `ItemWithMeta` + `itemSelect` + `ItemRow` + `toItemWithMeta` in `src/lib/db/items.ts` gain `fileUrl: string | null` so the new card avoids a per-card fetch (existing `baseRow` fixture in `items.test.ts` already had `fileUrl: null` so all 80 tests + `next build` still pass without test changes); spec file checked in at `context/features/image-display-spec.md`; live-verified via Playwright MCP at three breakpoints (3 cols @ ≥lg, 2 cols @ sm–md, 1 col @ mobile), confirmed `scale: 1.05` mid-hover with `transition-duration: 0.3s` on `transition-property: transform, translate, scale, rotate`, and confirmed the drawer still opens on thumbnail click with no console errors — Completed
- File list view — `/items/files` swaps the standard `ItemCard` grid for a Drive/Dropbox-style single-column list; new `ClickableFileRow` client component owns the row with an outer flex container (hover highlight) wrapping an inner `<button>` (icon + filename + size + date) plus a sibling `<a href="/api/files/[id]" download={fileName}>` that uses `e.stopPropagation()` so the download click can't bubble up — the `<a>` is intentionally a sibling, not a child, since nesting `<a>` inside `<button>` is invalid HTML; rows render an extension-aware Lucide icon via a `getExtension`-keyed map — `.pdf` → `FileText`, `.txt` → `FileType`, `.md` → `FileType2`, `.json` → `FileJson`, `.yaml`/`.yml`/`.toml`/`.ini` → `FileCode`, `.xml` → `FileCode2`, `.csv` → `FileSpreadsheet`, fallback → `File` — so the seven supported file types render distinctly; icon tint comes from `itemType.color` (`#6b7280` gray for the system `file` type); `formatBytes` from `upload-constraints.ts` formats the size, a "Mon DD, YYYY" `Intl.DateTimeFormat` formats `createdAt`; row clicks open the existing `ItemDrawer` with the row's `ItemWithMeta` payload; `/items/[type]/page.tsx` branches on `typeName === "file"` to render `<div className="flex flex-col gap-2">` of `ClickableFileRow`s — `image` and the default grid branches are unchanged; `ItemWithMeta` + `itemSelect` + `ItemRow` + `toItemWithMeta` in `src/lib/db/items.ts` gain `fileName: string | null`, `fileSize: number | null`, and `createdAt: Date` so the row renders without a second fetch, and the now-redundant `fileUrl`/`fileName`/`fileSize`/`createdAt` overrides were dropped from `ItemDetail` (still selected on detail since `itemSelect` spreads in); responsive: under `sm` the size · date pair stacks below the filename inside the button via `flex-col sm:flex-row`, with the download anchor staying right-aligned; existing `baseRow` fixture in `items.test.ts` already carried `fileName`/`fileSize`/`createdAt` so all 80 tests + `next build` pass with no test changes; spec file checked in at `context/features/file-display-spec.md`; live-verified via Playwright MCP at desktop (1440×900) and mobile (375×700) — drawer opens on row click, programmatic click on the download anchor returns `dialogsOpen: 0` (no drawer leak), `.md` file renders with `lucide-file-type-corner` (FileType2) distinct from `.pdf`'s `lucide-file-text`, and snippets/images pages still render unchanged with no console errors — Completed
- Quick-copy icon on cards — new `QuickCopyButton` client component (`Copy` ↔ `Check` icon swap for 1.2s on success, sonner toast on success/failure, `stopPropagation` so the click doesn't bubble to the open-drawer wrapper, timer cleanup on unmount) rendered as an absolutely-positioned sibling of the open-drawer `<button>` in `ClickableItemCard` (`<div className="relative group">` wrapper, copy button at `absolute bottom-3 right-3 opacity-0 group-hover:opacity-100 group-focus-within:opacity-100 [@media(hover:none)]:opacity-100` so it reveals on hover/focus for pointer devices and stays visible on touch viewports); siblings — not nested — because nesting `<button>` inside `<button>` would be invalid HTML; `getCopyText` reads from `item.itemType.name` to pick `content` for snippet/prompt/command/note, `url` for link, and `null` (button skipped) for file/image per user decision (avoids exposing the bucket URL on cards); `ItemWithMeta` + `itemSelect` + `ItemRow` + `toItemWithMeta` in `src/lib/db/items.ts` extended with `content` + `url` so the copy is synchronous inside the user-gesture handler (avoids losing the gesture across an async fetch in Safari) and the now-redundant `content`/`url` overrides were dropped from `ItemDetail` (still selected via the `itemSelect` spread); existing `baseRow` fixture in `items.test.ts` already carried `content` + `url` so all 80 tests + `next build` pass with no test changes; live-verified via Playwright MCP — `Copy useDebounce hook` wrote the full snippet, `Copy shadcn/ui` wrote `https://ui.shadcn.com/`, both with `dialogs: 0` (drawer did NOT open), the main card click still opens the drawer (`dialogs: 1`), hover transitioned `opacity: 0 → 1` on `transition-property: opacity`, `/items/files` and `/items/images` had `copyCount: 0`, and 0 console errors across `/dashboard`, `/items/snippets`, `/items/files`, `/items/images` — Completed
- Audit quick wins 2 — second batch of code-scanner audit fixes pulled from the 15-finding report (scope deliberately narrow: pure refactors + one query cap, no behavior change for end users); new `src/lib/auth-constants.ts` exports `BCRYPT_ROUNDS = 12` and is imported into `/api/auth/register` (was already 12), `/api/auth/reset-password` (was 10), and `/api/account/change-password` (was 10) — fixes a silent strength downgrade where a user who registered with cost-12 hashes would get re-hashed at cost 10 on reset / change (bcrypt verifies against the cost embedded in each stored hash, so existing cost-10 hashes still validate without a forced re-hash migration); local `formatBytes` in `item-drawer.tsx` (lines 58-62) deleted in favor of the canonical export from `@/lib/upload-constraints` (already Vitest-covered); `capitalize` + `parseTags` moved into `@/lib/utils.ts` and 3 local `capitalize` copies (sidebar, profile, items/[type] page) + 2 local `parseTags` copies (item-drawer, new-item-dialog) replaced with imports — the canonical `capitalize` adds an `if (!value) return value` empty-string guard the originals lacked (behavior preserved on `""` since `"".charAt(0).toUpperCase() + "".slice(1) === ""` already, but the guard makes intent clearer); the audit spec had also listed item-drawer and new-item-dialog as `capitalize` users, but those grep hits were actually the Tailwind `capitalize` CSS class — only 3 function copies existed; `getPinnedItemsForUser` in `src/lib/db/items.ts` gains an optional `limit = 50` parameter passed as `take` on the `findMany` (matches the existing `getRecentItemsForUser` signature), bounding the dashboard payload so a heavily-pinned account can't return an unbounded join; new Vitest coverage — `src/lib/__tests__/utils.test.ts` gains 5 `capitalize` cases (empty, single char, word, already-capitalized, leading-whitespace) and 5 `parseTags` cases (empty, commas-only, trim, dedupe with first-seen order, single tag); `src/lib/db/__tests__/items.test.ts` adds `findMany` to the prisma mock + 2 `getPinnedItemsForUser` cases (default `take: 50`, explicit override) — 92 tests + `next build` pass; out of scope and deferred to focused PRs: the Critical open-redirect on `callbackUrl` (#1), High N+1 in `fetchCollectionsWithMeta` (#3), High image R2 URL exposure (#4), and the remaining Medium/Low audit findings (callbackUrl page sanitization, `updateItemForUser` round-trip merge, `consumeVerificationToken` reorder, CSP headers, `getDemoUser` guard, item-drawer split, `token.id` cast) — Completed
- Audit quick wins 3 — third batch of code-scanner audit fixes, with the auth-related items (open redirect, token expiry, JWT cast) re-verified against Auth.js v5 docs via context7 MCP before implementing; new `src/lib/safe-redirect.ts` exports `safeRedirectPath(value, fallback = "/dashboard")` that rejects non-strings, empty values, anything not starting with `/`, protocol-relative URLs (`//evil.com`), and backslash-prefixed paths (`/\evil.com`) that some parsers treat as protocol-relative — wired into both `credentialsSignInAction` and `githubSignInAction` (Auth.js v5 docs confirm NextAuth does NOT auto-validate `redirectTo`); `consumeVerificationToken` and `consumePasswordResetToken` both had the same expiry-before-delete bug (audit only flagged the first; second was the same pattern in the same file) — reordered both to check `expires` before deleting the row so expired tokens keep surfacing as `"expired"` on retry instead of collapsing to `"invalid"` after the first attempt nukes the row; `getItemsForUserByTypeId` in `src/lib/db/items.ts` gains an optional `limit = 200` parameter passed as `take` (matches existing `getPinnedItemsForUser`/`getRecentItemsForUser` signatures), bounding the `/items/[type]` payload for users with thousands of rows; new `next.config.ts` `headers()` export applies `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, and `Referrer-Policy: strict-origin-when-cross-origin` to all routes (CSP deferred — needs planning for Monaco + Markdown); `confirmPassword` removed from the `RegisterForm` fetch payload AND its server-side equality check dropped in `/api/auth/register` since it was UX-only (client already validates) and never a security check; **bug surfaced and fixed during #8**: the JWT module augmentation in `src/types/next-auth.d.ts` targeted `next-auth/jwt`, but `next-auth/jwt` only re-exports from `@auth/core/jwt` — augmentations don't flow through re-exports, so `interface JWT { id: string }` was never merging and `token.id` was always `unknown` (from `Record<string, unknown>` extends), with the `as string` cast at `auth.config.ts:35` silently hiding it; fixed by augmenting `@auth/core/jwt` directly (with a comment explaining why), then dropped the cast and narrowed `user?.id` in the `jwt` callback since `user.id` is `string | undefined`; **dropped #11** (`getDemoUser` dead-code annotation) per user — the demo user is still used in production, not dev-only; new Vitest coverage — `src/lib/__tests__/safe-redirect.test.ts` (8 cases: null/undefined/number/object/empty/relative/javascript:/absolute/protocol-relative/backslash/valid path/custom fallback), `src/lib/__tests__/verification-token.test.ts` (9 cases across both consume helpers: empty token, no match, cross-purpose rejection, expired-without-delete, success-with-delete), `src/lib/db/__tests__/items.test.ts` gains 2 `getItemsForUserByTypeId` cases (default `take: 200`, explicit override); 111 tests + `next build` pass; out of scope and deferred to focused PRs: #4 R2 image proxy (architectural), #5 `fetchCollectionsWithMeta` rework, #6 `AUTH_URL` startup assert (needs env-policy decision), #7 `updateItemForUser` round-trip merge, #10 `item-drawer.tsx` split, #12 middleware matcher expansion — Completed
- Refactor Phase A — four low-risk consolidation moves from the inline refactor audit run after audit-quick-wins-3, pure code moves with no end-user behavior change except a small UX improvement on the long date format; new `EMAIL_REGEX` + `MIN_PASSWORD_LENGTH` exports in `src/lib/auth-constants.ts` (alongside existing `BCRYPT_ROUNDS`) replace 10 duplicate `const` declarations across 5 auth/profile components and 5 API routes (`register`, `forgot-password`, `reset-password`, `resend-verification`, `change-password`); new `src/components/items/_form-primitives.tsx` exports `Field` + `Textarea` (keeping new-item-dialog's `htmlFor?: string` superset signature) and replaces byte-identical copies inside both `item-drawer.tsx` and `new-item-dialog.tsx` (~80 duplicate lines dropped, plus the now-unused `Label` + `cn` imports cleaned out of item-drawer); new `src/components/ui/pending-button.tsx` exports `PendingButton({ pending, icon?, children, ...buttonProps })` that wraps the existing base-ui `Button` with a `LoaderCircle` spinner when pending and an optional non-spinner icon when not — adopted at **7 sites** (audit listed 4, but the same pattern existed at 3 more in the same change-area: sign-in submit + GitHub submit, register submit, change-password submit, delete-account submit, item-drawer Save, item-drawer Delete, new-item-dialog Create); new `src/lib/format-date.ts` exports `formatDateLong(value, now?)` which, **per user feedback during implementation**, returns `"Today"` / `"Yesterday"` for same-day / one-day-prior values in local time (computed via `startOfLocalDay` diff in `MS_PER_DAY` units to be DST-safe) and falls back to `"Month D, YYYY"` for anything else — applied at the item drawer's Created/Updated/Last-used `<dd>`s and the profile "Member since" row, replacing two ad-hoc `Intl.DateTimeFormat` instances; the originally-listed `clickable-file-row.tsx` was **excluded** from #4 after re-checking — it uses `month: "short"`, not `"long"`, so it's a different formatter and out of scope (same goes for `item-card.tsx`); `now` is injectable for deterministic tests; new Vitest coverage — `src/lib/__tests__/format-date.test.ts` (7 cases: today both edges of local day, yesterday across a month boundary, two-days-ago + future dates fall back to long format, ISO string input); `Field`/`Textarea`/`PendingButton` are components and explicitly out of scope per `coding-standards.md` Testing section (components excluded from Vitest); 118 tests + `next build` pass; net -103 lines (235 insertions, 185 deletions, of which the new code is the four primitives + 49-line test file); Phase B (audit #10, splitting the 828-line `item-drawer.tsx` into `ItemEditForm` / `ItemDrawerBody` / `DeleteItemDialog` / `ItemDrawerSkeleton`) deliberately deferred to a separate branch because it touches state plumbing — Completed
- Refactor Phase B — split the 778-line `src/components/items/item-drawer.tsx` (audit finding #10) into the orchestrator (262 lines after the split) plus 5 focused sibling files under `src/components/items/`: `item-edit-form.tsx` (137 lines — exports `ItemEditForm`, type `EditState`, helper `detailToEditState`), `item-drawer-body.tsx` (230 lines — exports `ItemDrawerBody`, internally houses `ItemContent` / `FileMetaRow` / `EmptyContent` / `Section`), `item-drawer-action-bars.tsx` (103 lines — exports `ViewActionBar` + `EditActionBar` together since they're small and conceptually paired), `delete-item-dialog.tsx` (61 lines), `item-drawer-skeleton.tsx` (31 lines); the three `TYPES_WITH_CONTENT` / `TYPES_WITH_LANGUAGE` / `TYPES_WITH_MARKDOWN` constants stay in the orchestrator as the single source of truth — both `ItemEditForm` and `ItemDrawerBody` now receive the resulting `showsContent` / `showsLanguage` / `showsMarkdown` / `showsUrl` booleans as props rather than re-deriving them from `detail.itemType.name` (previously `ItemContent` had its own local copies of those sets and re-derived the same booleans the orchestrator already had — drift impossible now); pure code move otherwise — no behavior change in the drawer; **bundled date-helper fix per user feedback**: extended `src/lib/format-date.ts` with `formatDateShort(value, now?)` (Today/Yesterday + fallback to `"Mon D"`, no year — wired into `src/components/dashboard/item-card.tsx`) and `formatDateMedium(value, now?)` (Today/Yesterday + fallback to `"Mon D, YYYY"` — wired into `src/components/items/clickable-file-row.tsx`), refactored the relative-label logic into a small private `relativeLabel()` helper shared by all three formatters; `/items/notes` (and every other `ItemCard` surface) + `/items/files` now show `"Today"` / `"Yesterday"` on recent items, matching what the drawer already did since Phase A; new Vitest coverage — `src/lib/__tests__/format-date.test.ts` gains 8 cases (4 `formatDateShort`, 4 `formatDateMedium`); per `coding-standards.md` Testing section, the 5 new component files have no Vitest coverage (components excluded from tests); 126 tests + `next build` pass — Completed
- Collection create — new `NewCollectionDialog` client component (`src/components/collections/new-collection-dialog.tsx`) wired to the top-bar "New Collection" button, replacing the previously-disabled placeholder; modal mirrors `NewItemDialog` UX with Name (required) + Description fields, reused `Field`/`Textarea` primitives, `PendingButton`, `useTransition`-driven pending state, sonner toasts on success/failure, form reset on close, and dismiss locked while pending; new `createCollectionAction` in `src/actions/collections.ts` returns `{ success, data | error }`, Zod-validates (trimmed required `name`, optional trimmed `description` via reused `normalizeOptional`), gates on `auth()` session, and delegates to new `createCollectionForUser` in `src/lib/db/collections.ts` which creates the row scoped by the session `userId` and returns a fresh `CollectionWithMeta` (`itemCount: 0`, `dominantType: null`, `types: []`) so the caller doesn't need a second fetch; `router.refresh()` after success re-renders the dashboard collections grid, sidebar Favorites/Recent + Collections stat counter, and any other server-rendered collection list; **bundled visual fix surfaced during browser verification**: `CollectionCard` (`src/components/dashboard/collection-card.tsx`) now uses `h-full` on the inner `Card` so cards lacking a types-icons row (any 0-item collection, including the newly created one) stretch to match the grid row height — was rendering shorter than full-content neighbors and visually reading as "narrower" because CSS Grid stretches the outer `<Link>` but the inner `Card` was only as tall as its content (also fixes a pre-existing inconsistency on other 0-types collections); new Vitest coverage — `src/actions/__tests__/collections.test.ts` (5 cases: no session, empty/whitespace name, normalization + whitespace-only description → null, kept trimmed non-empty description, generic db error) and `src/lib/db/__tests__/collections.test.ts` (2 cases: full create shape including `userId` scoping + exact `select` projection + `CollectionWithMeta` defaults, null description passes through); 133 tests + `next build` pass; live-verified via Playwright MCP — created "Playwright Test Collection" on `/dashboard`, dialog closed on success, all three server-rendered surfaces refreshed (Collections stat 5→6, dashboard grid card appeared first with "0 items", sidebar Recent updated with the new entry), 0 console errors, whitespace-only name correctly kept Create disabled, form reset on reopen — Completed
- Add item to collections — new multi-select Collections picker on both the `NewItemDialog` and the `ItemEditForm` (edit mode in the item drawer); new `CollectionsPicker` client component renders a wrapping list of `role="checkbox"` chip toggles styled like rounded outline badges (selected state shows a `Check` icon + primary-tinted background), with an inline empty-state ("No collections yet. Create one from the top bar…") when the user has no collections; new `UserCollectionsProvider` client context (`src/components/items/collections-context.tsx`) carries `{id, name}[]` for the session user, with the **`(dashboard)/layout.tsx`** now `async` + `auth()`-gated and prefetching once via new `getUserCollectionsList(userId)` so the picker has data ready on first dialog/drawer open without per-component fetching — `router.refresh()` already called by `createCollectionAction`/`createItemAction`/`updateItemAction` keeps the prefetched list in sync; `createItemAction` + `updateItemAction` Zod schemas grew an optional `collectionIds` field (trimmed + empty-filtered + deduped via `Array.from(new Set(...))`), both actions then call new `verifyCollectionsOwnedByUser(userId, collectionIds)` in `src/lib/db/collections.ts` before delegating — collections owned by a different user collapse to a `{ success: false, error: "Invalid collection" }` (blocks the cross-user attach attack the spec called out); db helpers `createItemForUser` + `updateItemForUser` both extended to take `collectionIds: string[]` — create writes `collections: { create: [...] }` in the same `prisma.item.create`, update adds a `prisma.itemCollection.deleteMany({ where: { itemId } })` to its existing `$transaction` (between the existing `tagsOnItems.deleteMany` and the `item.update`) and recreates the rows via `collections: { create: [...] }` inside the `item.update.data`; `ItemEditForm`'s `EditState` gains `collectionIds: string[]` seeded from the existing `detail.collections.map(c => c.id)` (the read-only "Collections" section in the drawer body was already reading the same shape); drawer's `handleSave` forwards `edit.collectionIds` in the payload; new Vitest coverage — `src/actions/__tests__/items.test.ts` gains 6 cases across both actions (`updateItemAction` & `createItemAction`: dedup-and-forward, cross-user reject, plus skip-when-empty for create), `src/lib/db/__tests__/items.test.ts` gains 2 cases for the new collection-link write shapes (create with deduped `collections.create`, update with `itemCollection.deleteMany` + reattach), `src/lib/db/__tests__/collections.test.ts` gains 4 cases for `getUserCollectionsList` (id+name select + asc sort) and `verifyCollectionsOwnedByUser` (empty short-circuit, all-owned, miss-detected); 133 → 144 tests + `next build` pass; collection pages (`/collections/[id]`) deliberately out of scope per spec — Completed
- Remove type badge from ItemCard + seed tags — dropped the colored `variant="outline"` type badge from `ItemCard` (left border color + Lucide icon already convey the type); the tags row now renders conditionally only when `item.tags.length > 0`; extended `scripts/seed.ts` so all 18 demo items carry realistic tags and the seeder wipes + recreates the demo user's data for idempotent re-runs; no schema change (global `Tag` model + `TagsOnItems` join table); `ImageThumbnailCard` and `ClickableFileRow` were already badge-free and unchanged; no new Vitest tests (component + seed script both out of scope); 144 tests + `next build` pass — Completed
- Collections pages — `/collections` lists all user collections as a `CollectionCard` grid with a "New Collection" button in the empty state; `/collections/[id]` shows collection metadata (name, favorite star, description, item count) + items partitioned into images/files/others, each rendered with its type-specific card (`ClickableImageCard`, `ClickableFileRow`, `ClickableItemCard`); both pages auth-gated, `notFound()` on unowned IDs, empty states, `generateMetadata` titles; new `getItemsForUserByCollectionId` + `getCollectionWithItemsForUser` db helpers; 5 new Vitest cases; 149 tests + `next build` pass — Completed
- Collection actions (Edit, Delete, Favorite) — Edit and Delete buttons on `/collections/[id]` header plus a 3-dot dropdown on collection cards (`/collections`, `/dashboard`); Edit opens a Name/Description dialog, Delete opens a confirmation dialog (items not deleted — only detached from the collection), Favorite stubbed with a toast; card click navigates to collection page with `stopPropagation` preventing dropdown clicks from triggering navigation; new `updateCollectionAction` + `deleteCollectionAction` with ownership-scoped `updateMany`/`deleteMany` db helpers; 11 new Vitest cases; 164 tests + `next build` pass — Completed
- Global search command palette — Cmd+K / Ctrl+K opens a `cmdk`-backed command palette with client-side fuzzy search across all items and collections; acronym-style subsequence matching (each char must be consecutive or at a word boundary, first field only) ranked exact > prefix > word-boundary > substring > subsequence; grouped results (Items ≤8, Collections ≤6) with type icons and item counts; keyboard navigation with loop wraparound; item selection opens existing `ItemDrawer`, collection selection navigates to `/collections/[id]`; `SearchPaletteProvider` owns the Cmd+K listener + context; `SearchTrigger` replaces the disabled top-bar input; search data prefetched in `(dashboard)/layout.tsx` (items ≤500, collections ≤200) via new `getUserSearchData`; new `fuzzyScore`/`fuzzySearch` utilities; 19 new Vitest tests (16 fuzzy-search + 3 search-db); 183 tests + `next build` pass — Completed
- Pagination — URL-based `?page=N` pagination on `/items/[type]`, `/collections/[id]` (items inside), and `/collections` (all-collections grid); new `src/lib/constants.ts` exporting `ITEMS_PER_PAGE = 21`, `COLLECTIONS_PER_PAGE = 21`, `DASHBOARD_COLLECTIONS_LIMIT = 6`, `DASHBOARD_RECENT_ITEMS_LIMIT = 10`; `src/lib/pagination.ts` re-exports constants + `parsePageParam`, `paginate`, `buildPageList`, `buildPageHref` utilities; new `src/components/ui/pagination.tsx` server-renderable component (returns null when totalPages ≤ 1, prev/next as `<Link>` or `aria-disabled` span, numbered pages with `aria-current="page"`, ellipsis via `buildPageList`); db helpers `getItemsForUserByTypeId` + `getItemsForUserByCollectionId` refactored to return `{ items, totalCount }` via parallel `Promise.all([findMany, count])`; new `getCollectionsPagedForUser` added; dashboard constants replaced with named imports; 23 new Vitest cases (20 pagination utility + 3 db); 206 tests + `next build` pass — Completed
- Settings page — new auth-gated `/settings` route inside `(dashboard)` shell (`auth()` check + `redirect("/sign-in?callbackUrl=/settings")`); reuses `getUserProfileById` to surface `hasPassword` + `email`; sidebar user dropdown (`SidebarUserMenu`) gains a "Settings" link with the lucide `Settings` icon between Profile and Sign out; `ChangePasswordDialog` + `DeleteAccountDialog` moved from `src/components/profile/` → `src/components/settings/` via `git mv` (preserving history), `src/components/profile/` directory removed since it was now empty; `/profile` page strips the action buttons (`<Separator>` + the conditional `flex-row` group) and the unused `Separator`/`ChangePasswordDialog`/`DeleteAccountDialog` imports, subtitle reworded to "View your account information and usage" since the page is now read-only; settings layout uses a vertical stacked layout per user feedback — `max-w-3xl` centered container, **Account** card (`ring-foreground/10`) with a bordered Password row (`flex-col items-start gap-3 rounded-lg border border-border p-4`, title + description stacked over the button; OAuth-only users get the same row structure with the GitHub copy and no button), **separate Danger zone** card (`ring-destructive/40`) with a destructive-styled Delete account row (`border-destructive/40 bg-destructive/5`, existing `variant="destructive"` button trigger preserved); no new server actions or utilities — pure UI/routing move; 206 tests + `next build` pass — Completed
- Editor preferences settings — new auth-gated Editor preferences card on `/settings` between Account and Danger zone, with font size (7 values 11–18), tab size (2/4/8), theme (`vs-dark` / `monokai` / `github-dark`), word wrap (default on), and minimap (default off) — changes auto-save on every interaction with no save button; new `editorPreferences` JSONB column on `User` via `prisma migrate dev --name add_user_editor_preferences`; new `src/lib/editor-preferences.ts` exports `FONT_SIZES`/`TAB_SIZES`/`EDITOR_THEMES`/`EditorPreferences` type/`DEFAULT_EDITOR_PREFERENCES`/Zod schema, plus a lenient `parseEditorPreferences(raw)` that fills in defaults for missing/invalid fields (used on every read so a partial or stale value keeps working); `src/lib/db/users.ts` gains `getUserEditorPreferences` + `updateUserEditorPreferences`; new `updateEditorPreferencesAction` in `src/actions/editor-preferences.ts` (`auth()` gate + Zod validation + delegate); new `EditorPreferencesProvider`/`useEditorPreferences` client context (`src/components/editor/editor-preferences-context.tsx`) holds state with optimistic updates, rolls back on server error, toasts on success/error via `useTransition`; `(dashboard)/layout.tsx` prefetches prefs alongside `getUserCollectionsList` + `getUserSearchData` and wraps the tree in `EditorPreferencesProvider`; `CodeEditor` no longer hard-codes `devstash-dark` — `beforeMount` registers all three themes via a new `src/components/editor/monaco-themes.ts` (each theme carries its own `chromeBg`/`chromeHeaderBg` so the outer container + header bar match the editor bg per theme), and Monaco options now derive `fontSize`/`tabSize`/`wordWrap`/`minimap`/`theme` from context; new `EditorPreferencesForm` in `src/components/settings/editor-preferences-form.tsx` uses the **base-ui-backed shadcn `Select`** primitive (`npx shadcn add select` — kept on the existing base-ui flavor that matches `dropdown-menu.tsx`/`dialog.tsx`/etc.) for the three dropdowns since a native `<select>` rendered as white-on-white in the OS popup; trigger renders the human label via `SelectValue`'s children render function `(v) => labelByValue[v]` so triggers show "VS Dark"/"2 spaces"/"15px" instead of raw values; two boolean toggles use a custom `role="switch"` button + sliding thumb (no base-ui Switch added since this is the only usage); new Vitest coverage — `src/lib/__tests__/editor-preferences.test.ts` (12 cases: 7 `parseEditorPreferences` paths + 5 schema paths) and `src/actions/__tests__/editor-preferences.test.ts` (6 cases: no session, invalid theme, invalid fontSize, non-object payload, forward+return, db throw); 224 tests + `next build` pass; live-verified via Playwright MCP — theme/font/minimap each persisted to Neon `development` branch (verified the JSON via `mcp__neon__run_sql`), reload preserved values, `/items/snippets` drawer rendered the github-dark bg `#0d1117` with 16px line font and 24px-wide minimap, 0 console errors; restored to defaults at end — Completed
- Favorites page — new auth-gated `/favorites` route inside `(dashboard)` shell listing all `isFavorite` items + collections in compact, dashboard-style stacked rows (matching Settings/Profile card language); page header is a bare filled-yellow `Star` icon + `Favorites (N)` with the total count inline (no background square per user feedback); each section sits under an external `font-mono uppercase tracking-wider text-xs` label ("ITEMS (N)" / "COLLECTIONS (N)") wrapped in an `overflow-hidden rounded-xl border border-border bg-card` container with `divide-y divide-border` rows — row layout is `flex items-center justify-between gap-4 px-4 py-2.5 hover:bg-muted/50` with type icon (tinted via `iconMap[item.itemType.icon]` + `style={{ color: itemType.color }}`) + mono title on the left, type-colored pill badge + relative date on the right; badges are `rounded-full border px-2.5` with inline `color`, `borderColor: ${color}66` (40% alpha), `backgroundColor: ${color}1a` (10% alpha) so each system type pops in its own hue (blue Snippet, orange Command, emerald Link, purple Prompt, etc.) — collection badges use `dominantType.color`; new `FavoriteItemRow` client wrapper owns the existing `ItemDrawer` open state, collection rows are plain `<Link href="/collections/[id]">` straight to the page (no client wrapper needed since collections don't open a drawer); single page-level empty-state card shown only when **both** sections are zero; new db helpers — `getFavoriteItemsForUser` in `src/lib/db/items.ts` (`where: { userId, isFavorite: true }`, `orderBy: { updatedAt: "desc" }`, full `itemSelect`) and `getFavoriteCollectionsForUser` in `src/lib/db/collections.ts` (a thin wrapper around `fetchCollectionsWithMeta` which gained a new optional `isFavorite?: boolean` option that threads into the existing `where` clause — reuses the N+1-safe dominant-type aggregation untouched); `TopBar` (`src/components/dashboard/top-bar.tsx`) gains a `Star` icon `<Link href="/favorites">` styled as a `size-9` icon button immediately before `NewCollectionDialog`; new `formatDateRelative(value, now?)` exported from `src/lib/format-date.ts` returns `"Today"` / `"Yesterday"` / `"N days ago"` (2–6) / `"1 week ago"` / `"N weeks ago"` (14–27) / `"N months ago"` (28–364) with `"Mon D, YYYY"` fallback for older dates and any future date, sharing the existing `startOfLocalDay` helper so it stays DST-safe; `scripts/seed.ts` extended with an optional `isFavorite?: boolean` on the `ItemSeed` shape + `isFavorite: item.isFavorite ?? false` in the seeder's create call, then 6 demo items flagged so re-seeding gives the page realistic content out-of-the-box; new Vitest coverage — `src/lib/__tests__/format-date.test.ts` gains 8 `formatDateRelative` cases (today, yesterday, 2 vs 6 days ago, 1 week vs N weeks ago boundaries, 1 vs N months ago, very-old fallback, future fallback); 224 → 232 tests + `next build` pass; bundled commits on the same branch — `chore: reformat dropdown-menu primitive` (pure semicolon/multi-line cleanup of `src/components/ui/dropdown-menu.tsx`) and `chore: refresh nextjs-codebase-auditor agent memory` (renamed `.claude/agent-memory/nextjs-codebase-auditor/project_conventions.md` → `project-conventions.md` with refreshed content and updated MEMORY.md pointer); favorite-toggle wiring deferred per user (action bar star still stubbed) — Completed
- Favorite toggle — wired up the previously-stubbed favorite controls across all surfaces; new `setItemFavoriteAction` in `src/actions/items.ts` + `setItemFavoriteForUser` in `src/lib/db/items.ts` (ownership-scoped `prisma.item.updateMany({ where: { id, userId }, data: { isFavorite } })` so wrong-owner and missing-id collapse to `count === 0`), plus the matching `setCollectionFavoriteAction` / `setCollectionFavoriteForUser` pair in the collections counterparts; item drawer's Favorite star (`item-drawer-action-bars.tsx` `ViewActionBar` now takes `isFavorite`/`favoritePending`/`onToggleFavorite` props and `aria-pressed`), collection page header star (`collection-actions.tsx`), and collection card 3-dot menu (`collection-card-menu.tsx`) all swap their `toast.info("coming soon")` stubs for the new actions with optimistic update + previous-value rollback on error + `router.refresh()` on success (matches the `EditorPreferencesProvider` pattern); new `QuickFavoriteButton` client component (`src/components/items/quick-favorite-button.tsx`) renders as a `size-7` rounded outline button with the same translucent-backdrop styling as `QuickCopyButton`, uses `event.stopPropagation()` + `event.preventDefault()` to never bubble to the open-drawer wrapper, calls `setItemFavoriteAction` with the same optimistic+rollback pattern, and `disabled={pending}` blocks rapid double-click; wired into all three card surfaces — `ClickableItemCard` (`top-3 right-3`, paired with the existing bottom-right `QuickCopyButton`), `ClickableImageCard` (`top-2 right-2`), and `ClickableFileRow` (sibling before the Download anchor) — each with conditional opacity classes so the button stays always visible when `isFavorite === true` and reveals on `group-hover` / `group-focus-within` / `[@media(hover:none)]` otherwise; the now-redundant inline `Star` indicator next to the title was dropped from `ItemCard` (only the `Pin` indicator remains) and from `ImageThumbnailCard` (its pin overlay moved from `right-2 top-2` to `left-2 top-2` to free the top-right corner for the favorite button); the `useEffect` re-syncing `isFavorite` from props on every parent refresh in each client surface keeps things consistent after `router.refresh()` lands; new Vitest coverage — `src/actions/__tests__/items.test.ts` + `src/actions/__tests__/collections.test.ts` each gain 6 cases for the new action (no session, empty id, not-found, success true, success false, generic-error) and `src/lib/db/__tests__/items.test.ts` + `src/lib/db/__tests__/collections.test.ts` each gain 3 cases for the new db helper (scoping shape with `data: { isFavorite: true }`, no-match returns false, `isFavorite=false` passthrough); bundled mock additions — `prisma.item.updateMany` added to the items prisma mock; 232 → 250 tests + `next build` pass — Completed
- Favorites page client-side sorting — `/favorites` swaps the original always-newest server-side ordering for two **independent** in-memory sort controls (one per section); new `FavoritesView` client component (`src/components/favorites/favorites-view.tsx`) owns two `useState<FavoritesSort>` slices (`itemsSort` + `collectionsSort`, both default `"newest"`) and renders a small base-ui `Select` (same primitive as `EditorPreferencesForm`) inside each section header — control is hidden when a section has ≤1 row; the page (`src/app/(dashboard)/favorites/page.tsx`) stays a server component fetching `getFavoriteItemsForUser` + `getFavoriteCollectionsForUser` and just hands the arrays to `<FavoritesView />`; new `src/lib/favorites-sort.ts` exports the five-mode union `FavoritesSort = "newest" | "oldest" | "name-asc" | "name-desc" | "type"` with labels (**Newest** / **Oldest** / **Name (A–Z)** / **Name (Z–A)** / **Item type**), `DEFAULT_FAVORITES_SORT = "newest"`, and two pure helpers — `sortFavoriteItems` / `sortFavoriteCollections` — that copy-then-sort (no input mutation), use case-insensitive `localeCompare` with `sensitivity: "base"` for both name modes, and under `"type"` group by `itemType.name` / `dominantType?.name` then fall back to most-recently-updated within a group (collections with null `dominantType` sort last); **bundled query cap surfaced during review**: both favorites db helpers were previously unbounded `findMany`s (a heavily-starred account would render every row in one server response), so `getFavoriteItemsForUser(userId, limit = FAVORITES_ITEMS_LIMIT)` now passes `take: limit` to its `findMany` and `getFavoriteCollectionsForUser(userId, limit = FAVORITES_COLLECTIONS_LIMIT)` threads through `fetchCollectionsWithMeta`'s existing `take` option, with the two new constants (both `200`) added to `src/lib/constants.ts` alongside the existing `ITEMS_PER_PAGE` / `COLLECTIONS_PER_PAGE` / dashboard limits — pattern matches `getPinnedItemsForUser` and `getItemsForUserByTypeId`, so raising/lowering the cap is a one-line edit; new Vitest coverage — `src/lib/__tests__/favorites-sort.test.ts` (10 cases: 5 modes × items + 5 modes × collections covering newest, oldest, name-asc, name-desc, type with the null-dominantType-sorts-last invariant, plus no-mutation + empty-array guarantees), and `src/lib/db/__tests__/items.test.ts` + `src/lib/db/__tests__/collections.test.ts` each gain 2 cases for the new helpers (default `take: 200` shape + explicit-override); 250 → 268 tests + `next build` pass — Completed
- Pinned items — wired up the previously-stubbed Pin button in `ItemDrawer`'s `ViewActionBar` (`isPinned` / `pinPending` / `onTogglePin` props + `aria-pressed` + `fill-current` glyph when pinned) so toggling pin state finally persists; new `setItemPinnedAction` in `src/actions/items.ts` + `setItemPinnedForUser` in `src/lib/db/items.ts` (ownership-scoped `prisma.item.updateMany({ where: { id, userId }, data: { isPinned } })` mirroring the favorite-toggle pair so wrong-owner / missing-id collapses to `count === 0`); drawer's `handleTogglePin` uses the same optimistic-update + previous-value-rollback + `router.refresh()` pattern as `handleToggleFavorite`, with a `useEffect` re-syncing `isPinned` from `cardItem.isPinned` on parent refresh; listings now sort pinned items to the top — `getRecentItemsForUser`, `getItemsForUserByTypeId`, and `getItemsForUserByCollectionId` all gain `{ isPinned: "desc" }` as the first `orderBy` clause before the existing `lastUsedAt desc nulls:last` / `updatedAt desc` fallbacks (dashboard pinned section was already wired via `getPinnedItemsForUser`); **bundled card layout reflow surfaced during browser verification**: the `QuickFavoriteButton` was previously absolutely-positioned at `top-3 right-3` on `ClickableItemCard`, which collided with the date span at the top-right of `ItemCard` — moved the favorite button inline into the title row (between the title and the pin glyph) with `gap-2` + `ml-1` margin on the pin, and switched the open-drawer wrapper from `<button>` → `<div role="button" tabIndex={0}>` with explicit `onClick` + `onKeyDown` (Enter/Space gated by `target === currentTarget` so nested-control keys don't bubble through) since `<button>` can't be nested inside `<button>` per HTML spec; `QuickCopyButton` stays absolutely-positioned at `bottom-3 right-3` (still a sibling of the role="button" div, no nesting issue); the spec's "Pin indicator on `ItemCard` stays a static badge" rule is preserved — only the drawer Pin became interactive, the card-surface `Pin` glyph remains a non-clickable indicator; no QuickPinButton added per spec; new Vitest coverage — `src/actions/__tests__/items.test.ts` gains 6 `setItemPinnedAction` cases (no session, empty id, not-found, success true, success false, generic-error), `src/lib/db/__tests__/items.test.ts` gains 3 `setItemPinnedForUser` cases (scoping shape with `data: { isPinned: true }`, no-match returns false, `isPinned=false` passthrough) and 3 orderBy assertions (`getItemsForUserByTypeId` / `getItemsForUserByCollectionId` / `getRecentItemsForUser` all verify the new 3-tuple `[isPinned desc, lastUsedAt desc nulls last, updatedAt desc]`); 268 → 280 tests + `next build` pass; live-verified via Playwright MCP — card body click opens drawer (`drawerOpen: 1`), focused card + Enter opens drawer, favorite-star click does NOT open drawer (`drawerOpen: 0`, bubbling blocked by `stopPropagation`), `tabIndex="0"` confirmed on the role="button" div, 0 console errors — Completed
- Homepage mockup — new static marketing prototype at `prototypes/homepage/` (3 files: `index.html`, `styles.css`, `script.js`) — deliberately outside the Next.js app at `src/app/page.tsx` so it can iterate without touching the build; spec checked in at `context/features/homepage-mockup-spec.md`; layout is 7 sections — fixed Navigation, Hero (text + three-column "chaos → arrow → order" visual), 6-card Features grid, AI Section (two-column with `Pro Feature` badge + checklist + code-editor mockup with the `useDebounce` snippet + AI-generated tag pills with staggered `tag-in` keyframe entrance), Pricing (Monthly/Yearly toggle, Free vs Pro cards with "Most Popular" badge), CTA, Footer; hero visual is the centerpiece — left chaos container (figure with dashed border + dim-radial backdrop) renders 8 inline-SVG knowledge-source glyphs (Notion, GitHub, Slack, VS Code, Browser, Terminal, Text file, Bookmark) animated via a new `ChaosIcon` class in `script.js` using `requestAnimationFrame` with Verlet-ish drift + 0.985 friction + min/max speed clamps (0.25 floor + 4.5 cap), 4-wall bounce on the stage bounds, and `pointermove`-driven mouse repel within a 110px radius (force scales linearly with proximity), plus a subtle `Math.sin(t/700)` scale pulse per icon for added life; center is a CSS `arrow-pulse` keyframe (1.8s ease-in-out infinite, scale 1→1.12 with opacity 0.85→1) on an inline SVG arrow tinted `--primary` with a blue drop-shadow; right is the dashboard preview — a `figure` with sidebar (DevStash logo + nav items with colored dots matching the item-type palette + a COLLECTIONS sub-group) and a 3-col grid of dash-cards with `border-top: 3px solid var(--accent)` per type; color palette follows the **spec** (snippet `#3b82f6` / prompt `#f59e0b` / command `#06b6d4` / note `#22c55e` / file `#64748b` / image `#ec4899` / url `#6366f1`) — **note the spec's prompt + command values differ from the live app** (app uses prompt purple `#8b5cf6` + command orange `#f97316`); CSS scroll-reveal — `.reveal` elements (16 sections + sub-sections) get `opacity: 0` + `translateY(16px)` and the JS hooks an `IntersectionObserver` with `rootMargin: 0px 0px -10% 0px` + threshold 0.08 to add `is-visible` (with `unobserve` to avoid re-fires); navbar — fixed top with `backdrop-filter: blur(12px)` + 55% bg, gains an `is-scrolled` class past 20px scroll that bumps bg to 88% and adds a bottom border; pricing toggle — vanilla JS swaps `textContent` from `data-monthly`/`data-yearly` attrs on the Pro card's price ($8 / $6), period (/ month / / month, billed yearly), and billed-note ("Billed monthly" / "$72 billed yearly") nodes, and flips `aria-selected` on the two `role="tab"` buttons; full `prefers-reduced-motion: reduce` support — reveal elements start visible with transition disabled, arrow animation skipped, chaos icons render at their spawn positions without rAF; resilience — `ResizeObserver` watches the chaos stage so icon bounds + clamped positions track responsive resizes, `visibilitychange` pauses the rAF loop when the tab is hidden, and a `pointerleave` on the stage flips `mouse.active = false` so off-stage cursor doesn't keep repelling; responsive — at `≤1024px` features collapse 3→2, hero visual stacks vertically, and the arrow rotates 90° to point down (keyframe rewritten to keep the rotation while pulsing); at `≤760px` the nav links + Sign In hide (leaving logo + Get Started), features+pricing become single-column, footer brand spans full width across a 2-col grid, dash grid drops to 2 cols; at `≤480px` the dashboard sidebar reflows to horizontal chip-style nav rows and the dash collapses to a single column; per `coding-standards.md` Testing section, no Vitest additions — the prototype is pure HTML/CSS/JS with no server actions or utilities under `src/`; all 280 tests + `next build` still pass (changes are entirely outside the Next.js compile target); live-verified via Playwright MCP at desktop 1440×900 + mobile 390×800 — 8/8 chaos icons animate (positions changed within 600ms), arrow `animationName === "arrow-pulse"`, nav adds `is-scrolled` past 20px, pricing toggle $8 → $6 → $8, mouse-repel pointermove caused 87px total icon delta over 500ms, only console error was the harmless `favicon.ico` 404 — Completed
- Homepage (real app) — replaced the `<h1>Devstash</h1>` placeholder at `src/app/page.tsx` with the production marketing homepage, porting the 7 sections from `prototypes/homepage/` to Tailwind v4 + shadcn primitives; `page.tsx` is a server component that calls `auth()` once and threads `isAuthenticated` into the nav, hero, pricing, and CTA so every "Start for free"-style CTA flips to "Open dashboard" → `/dashboard` when signed in (the Sign In ghost button hides too); new components under `src/components/marketing/` — `marketing-nav.tsx` (client, `useEffect` scroll listener toggling `data-scrolled` past 20px → backdrop intensifies via `data-[scrolled=true]:bg-background/90 data-[scrolled=true]:border-border` instead of always-on opacity), `hero-section.tsx` (server, composes chaos stage + arrow + dashboard preview in a `grid-cols-1 lg:grid-cols-[1fr_auto_1.15fr]` layout), `chaos-stage.tsx` (client, full `ChaosIcon` class port + rAF loop + 0.985 friction + 4-wall bounce + 110px-radius pointer repel + `Math.sin(t/700)` pulse + `ResizeObserver` bounds tracking + `visibilitychange` pause + `prefers-reduced-motion` static-spawn fallback; module-level `CHAOS_ICONS` const with inline SVG strings for 8 knowledge sources; cleanup function in `useEffect` removes DOM nodes + cancels rAF + disconnects observers), `dashboard-preview.tsx` (server, no state, color dots driven by `SYSTEM_TYPE_COLORS`), `features-section.tsx` (server, 6 cards with per-card `--feat-accent` inline style + hover `::before`-style absolute span with `color-mix` accent glow + `hover:-translate-y-0.5`), `ai-section.tsx` (server, checklist + editor mock with token-colored spans + 5 AI tag pills using `animate-tag-in` with `animationDelay: i * 120ms` inline), `pricing-section.tsx` (client, `useState<"monthly" | "yearly">` driving a `PRO_PRICE` lookup table — both prices render via mapped strings so structure stays stable, not JSX-conditional swap; Pro CTA points to `/register` for both auth states with a `// TODO: point Pro CTA at /settings#billing once billing is wired up.` comment since billing isn't done), `cta-section.tsx` (server, auth-aware), `marketing-footer.tsx` (server, `new Date().getFullYear()`, Product column has real `#features` / `#pricing` / `#ai` hashes + Resources/Company arrays are `#` placeholders with `// TODO: real links when these routes exist` comments), `reveal.tsx` (client, per-component `IntersectionObserver` with `rootMargin: "0px 0px -10% 0px"` + threshold 0.08, unobserves after first intersect, accepts `as`/`className`/`style`/spread HTML attrs so Reveal can wrap section/article/div), and `logo-mark.tsx` (server, shared blue→indigo gradient mark reused by nav + footer); `globals.css` gains `.marketing-reveal` / `.marketing-reveal.is-visible` (opacity + translateY transition), `.animate-arrow-pulse` (scale 1→1.12 + opacity pulse), `.animate-tag-in` (translateY entrance) under `@layer utilities`, all wrapped in a `@media (prefers-reduced-motion: reduce)` no-op block; `SYSTEM_TYPE_COLORS` moved into `src/lib/constants.ts` (single source of truth per user feedback — original plan was a new `system-type-colors.ts` file but user pushed back to keep constants in one place) and features cards now show the live-app palette so prompt reads purple `#8b5cf6` + command reads orange `#f97316` (not the prototype's amber/cyan); spec deviation #1 — `dynamic(..., { ssr: false })` from a server component isn't allowed in Next 16, but since `ChaosStage` is a `"use client"` module whose render body is just an empty `<div>` (all DOM creation happens in `useEffect`), a direct import is SSR-safe and equivalent; spec deviation #2 — used `.marketing-reveal` (not `.reveal`) to avoid namespace collisions; **bundled arrow-rotation fix surfaced during browser verification**: the original `animate-arrow-pulse-vertical lg:animate-arrow-pulse` two-class approach didn't work because both utilities set `animation:` shorthand and Tailwind v4 emitted them where the vertical one won everywhere regardless of media query; fixed by splitting concerns — outer wrapper handles rotation via `rotate-90 lg:rotate-0` (a CSS `rotate:` property that doesn't compose with `transform`-based animations), inner div handles the scale-only `animate-arrow-pulse` — so the keyframe stays single-purpose and the responsive rotate doesn't fight the animation; dropped the now-unused `arrow-pulse-vertical` keyframes from `globals.css`; per `coding-standards.md` Testing section, components are out of scope for Vitest — no new tests required, all 280 existing tests + `next build` pass; live-verified via Playwright MCP at desktop 1440×900 + mobile 390×800 — at lg the arrow wrapper `rotate` computed as `none` (horizontal) with `animation-name: arrow-pulse`, at mobile `rotate: 90deg` (vertical), nav `data-scrolled` flips from `"false"` → `"true"` after `scrollTo(0, 50)`, 8 chaos icons animate (transform changes mid-frame), pricing Yearly click flipped `$8 → $6` + `Billed monthly → $72 billed yearly` + `aria-selected` flipped on both tabs, 0 console errors across the full scroll; prototype files at `prototypes/homepage/` left intact for reference per spec — Completed
- Visual polish — fonts, logo, accent, hero mockup — global font swap from the (effectively dead) `Geist`/`Geist_Mono` `next/font/google` setup to **Inter** via `next/font/google` with `variable: "--font-sans"` so the `@theme inline { --font-sans: var(--font-sans) }` chain in `globals.css` actually resolves at runtime (previously `--font-geist-sans` was set on `<html>` but never referenced anywhere, so the app silently fell back to browser-default sans — Inter now actually renders); unified brand mark — dashboard `TopBar` (`src/components/dashboard/top-bar.tsx`) gains a `<Link href="/dashboard">` with the homepage `DevStashLogoMark` + "DevStash" wordmark (wordmark `hidden sm:inline` so the mobile bar keeps just the icon), and the legacy "DS" `bg-primary` square in `DashboardSidebar` (`src/components/dashboard/sidebar.tsx`) was dropped — the `SidebarHeader` stays at `h-14` with the bottom border for visual alignment with the TopBar but is empty in expanded mode and renders just the `DevStashLogoMark` in collapsed/icon mode (via `group-data-[collapsible=icon]:inline-flex` on the link); accent swap — every `#6366f1` (indigo) on the marketing surfaces became `#3b82f6` (snippet blue, `SYSTEM_TYPE_COLORS.snippet`): `LogoMark` gradient `#3b82f6 → #6366f1` → `#60a5fa → #3b82f6` (sky → snippet, kept a 2-stop blue gradient instead of collapsing to a flat color), hero title text gradient `#3b82f6 0% → #6366f1 50% → #f59e0b 100%` → `#60a5fa 0% → #3b82f6 50% → #f59e0b 100%` (anchored on snippet blue, kept the amber finish), pricing "Most Popular" badge gradient and dashboard preview sidebar logo gradient both updated to match `LogoMark`, AI section background tint `rgba(99, 102, 241, 0.04)` → `rgba(59, 130, 246, 0.04)`; the `#c084fc`/`#f472b6` purples/pinks inside the AI section's code-editor mockup were deliberately left alone — they're syntax-highlight semantic colors (keyword/operator), not brand accents; Features section (`src/components/marketing/features-section.tsx`) gains `bg-card/40 border-y border-border` (was `border-t` only on a transparent bg) for a subtle lift between Hero and AI — `bg-card` at 40% opacity sits between `--background` (`oklch 0.145`) and `--card` (`oklch 0.205`) so the section reads as one shade up from the page bg, with the inner feature cards still visibly lifted on top; hero `DashboardPreview` mockup (`src/components/marketing/dashboard-preview.tsx`) — dropped the `meta` field from `DashCard` type and removed the type-name `<div className="text-[10px] text-muted-foreground">` footer ("snippet · ts", "prompt", etc.), then removed `min-h-16 flex flex-col justify-between gap-1.5` from the card markup so each card is now just its `borderTop` accent + title row at natural height, and grew the grid from **6 → 12 cards (4 rows × 3 cols)** by appending six new realistic entries: `useThrottle hook` / `git rebase -i` / `API design notes` (first pass) then `Refactor prompt` / `Tailwind v4 docs` / `schema.prisma` (second pass after user asked for another row to tighten vertical space) — covers all 7 system types across the grid; no functional/routing/data/API/test changes — components remain out of scope for Vitest per `coding-standards.md`, all 280 tests + `next build` pass — Completed
- TopBar mobile oversaturation fix + a11y/UX easy wins — at 390px the TopBar's right cluster (Favorites star + labeled "New Collection" + labeled "New Item") squeezed the centered `SearchTrigger` pill to ~50–80px effective width per the ui-reviewer agent's report; rebuilt the bar so on mobile the DevStash logo+wordmark are hidden via `hidden sm:inline-flex` on the `<Link>` (both visible at sm+), the search pill is hidden via `hidden ... sm:flex` on the centering wrapper (still `flex-1` from sm so the pill takes the middle band on desktop), a `flex-1 sm:hidden` spacer pushes the right cluster to the edge on mobile, and a new icon-only `SearchTriggerIcon` (size-9 ghost, paired with the favorites star treatment) is appended to the right cluster with `sm:hidden` so it sits inline with the other icon affordances rather than centered in dead space; the two separate triggers (`<NewCollectionDialog />` + `<NewItemDialog />`) collapse into a single primary `+` dropdown via new `TopBarCreateMenu` (`src/components/dashboard/top-bar-create-menu.tsx`, base-ui `DropdownMenu` from `@/components/ui/dropdown-menu` — same primitive `SidebarUserMenu` + `CollectionCardMenu` use) that owns both dialog `open` states and renders the dialogs as controlled siblings — items "New item" + "New collection" with their existing `Plus` / `FolderPlus` icons; to make that work without nesting a `DialogTrigger` inside a `DropdownMenuItem` (which fails to sequence the menu close vs. dialog open cleanly), `NewItemDialog` + `NewCollectionDialog` both gain optional `open?: boolean` + `onOpenChange?: (open: boolean) => void` props that override the existing internal `useState`-driven `open`/`setOpen` when provided — when controlled, the `<DialogTrigger>` is skipped entirely (`!controlled && <DialogTrigger render={triggerNode} />`) so the parent component drives open state via React; every existing page-level call-site (`<NewItemDialog />` on `/items/[type]` with `initialType=` + custom `trigger=`, `<NewCollectionDialog />` previously in the TopBar elsewhere) keeps working unchanged since the new props are optional and the internal-state branch is the default; `SearchTrigger` is now also platform-aware — new internal `useShortcutLabel()` hook defaults to `⌘K` (the historic value) on SSR/initial render and switches to `Ctrl+K` in a `useEffect` when `/Mac/.test(navigator.platform)` + `userAgent` both miss, so Windows/Linux users finally see the correct binding (brief render-flicker on first paint is acceptable vs. an SSR/CSR hydration mismatch); shortcut label threads into both the pill's visible `<kbd>` AND the icon button's `title` + `aria-label`; **bundled UI-review easy wins** (5 of the 25 findings from the original review, picked as the no-risk-to-visual subset): **(#4)** `ChaosStage` container swaps `aria-hidden="true"` for `role="img" aria-label="Scattered developer knowledge sources"` and child icon spans now get `aria-hidden="true"` set in the dynamic-create loop — now communicates the hero metaphor to screen readers instead of silently hiding it; **(#6/11)** marketing footer's 9 placeholder `href="#"` links (Changelog in Product column + Resources array + Company array) gained `aria-disabled="true" tabIndex={-1}` + `cursor-not-allowed text-sm text-muted-foreground opacity-50 pointer-events-none` so they read as disabled until real routes exist; **(#19)** favorites collection `<Link>` rows fixed a real a11y regression — `focus-visible:outline-none` was set without a ring replacement, so keyboard nav had no focus indicator at all, now adds `focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-ring` matching the app's standard pattern; **(#20)** `/settings` Password row gains `opacity-70` conditional on `!profile.hasPassword` (via new `cn` import) so OAuth-only users see the row dim and clearly non-interactive instead of identical to the actionable Change-Password state; **(#24)** item drawer's `<Section title="Content">` now derives title contextually from `detail.contentType` — `FILE` → `"File"`, `URL` → `"Link"`, `TEXT` → `"Content"` — verified via Playwright on `/items/links` (drawer sections now: Description, Link, Tags, Collections, Details); excluded as risky-for-visual: AI badge contrast (#2, would change brand color), Stats card icon resize (#10), Image gallery `next/image` migration (#16, needs config), Image-card favorite button bg (#17, dark/light split), Drawer action bar flex-wrap (#22, layout shift); per `coding-standards.md` Testing section, components are out of scope for Vitest — no test additions, all 280 existing tests + `next build` pass; live-verified via Playwright MCP at desktop (1440×900) and mobile (390×800) — desktop renders logo + pill search with `Ctrl+K` + favorites + `+` dropdown, `+` opens menu with both entries, "New collection" creates "TopBar Verify Collection" (verified via `mcp__neon__run_sql` on the `development` branch — name + description + createdAt all match), Collections stat went 5→6, dialog closed cleanly after success; mobile (390): DOM check confirms logo `offsetParent === null` (hidden), pill search `offsetParent === null` (hidden), icon search visible at x=257 / favorites at x=299 / `+` at x=341 — all four icon buttons fit with breathing room in the 366px usable header width; mobile search icon click opens the cmdk palette (`paletteFound: true`, `dialog title: "Search"`); 0 console errors across `/dashboard` + `/items/links` + mobile dropdown interactions — Completed
- Marketing nav on auth pages + dashboard logo in sidebar header — rendered the homepage `MarketingNav` at the top of `/sign-in`, `/register`, `/forgot-password`, and `/reset-password` (and `/verify-email`, which lives in the same `(auth)` group) via a single-file edit to the shared `src/app/(auth)/layout.tsx`; the layout is now `async`, calls `auth()` to thread `isAuthenticated` into the nav (so the CTA flips between "Sign In + Get Started" and "Go to dashboard" correctly even though all four pages already redirect logged-in users), and bumps the card wrapper's top padding from `py-10` to `pt-24 pb-10 sm:pt-28` so the centered auth card always clears the `fixed inset-x-0 top-0` nav on short viewports (verified down to 600px viewport height — 31px gap remained between nav bottom and card top); bundled fix to a dashboard chrome regression surfaced during verification: the previous visual-polish PR put `DevStashLogoMark` + "DevStash" wordmark in the dashboard `TopBar` on desktop, leaving the `SidebarHeader` empty in expanded mode and the brand floating in the wrong spot — moved it back into `SidebarHeader` (always `inline-flex`, wordmark hidden in icon mode via `group-data-[collapsible=icon]:hidden` on the `<span>`) so the brand sits in the top-left corner above the sidebar nav where it belongs, with the now-unused `DevStashLogoMark` import + the `<Link>` block (lines 17-23 of `top-bar.tsx`) dropped from the topbar entirely; mobile drawer also picks up the logo + wordmark in its header as a positive side-effect (previously empty); also added the previously-untracked `.claude/agents/ui-reviewer.md` to the commit (was in `git status` as `??` since before this branch — the other two agents in `.claude/agents/` are committed, so the convention is to track agent files); per `coding-standards.md` Testing section, components are out of scope for Vitest — no test additions, all 280 existing tests + `next build` pass; live-verified via Playwright MCP at desktop (1440×900), mobile (390×800), and a 390×600 short viewport — all four auth pages render the nav with proper card clearance, desktop dashboard now shows logo + wordmark at x=16/y=15 (top-left above "TYPES"), collapsed icon-mode sidebar shows just the logo mark centered, mobile dashboard topbar is clean (no logo), mobile drawer shows logo + wordmark when opened, 0 console errors — Completed
- Marketing nav anchor links fix — bug introduced by the previous "Marketing nav on auth pages" feature: the three nav anchor links in `src/components/marketing/marketing-nav.tsx` used bare `href="#features"` / `href="#ai"` / `href="#pricing"`, which browsers resolve against the current pathname — so on every auth page (`/sign-in`, `/register`, `/forgot-password`, `/reset-password`, `/verify-email`) the links navigated to `/sign-in#features` (etc.) and the homepage sections never loaded; one-file fix swapping all three to absolute `/#features` / `/#ai` / `/#pricing` so the link always targets `/` first and then scrolls to the anchor — on the homepage itself the same-path-with-hash href still triggers the browser's in-page anchor handling and just scrolls without a full reload, so the original on-`/` behavior is preserved; spec checked in at `context/fixes/fix-marketing-links.md`; the Sign In / Get Started / Go to dashboard CTAs already used absolute paths and were left untouched; per `coding-standards.md` Testing section components are out of scope for Vitest — no test additions, all 280 existing tests + `next build` still pass — Completed
- Stripe Phase 1 — Core infrastructure for upcoming Pro subscriptions ($8/mo, $72/yr); intentionally an "invisible commit" with no user-facing change — sets up SDK + types + session shape so Phase 2 webhooks have a settled foundation; new dependency `stripe@^17.7.0` (npm resolved 17.7.0 from the spec's `^17.5.0` range); new `src/lib/stripe.ts` — `import "server-only"`, lazy `getStripe()` singleton keyed by `STRIPE_SECRET_KEY` (cache invalidates if the env key changes between calls), pinned `apiVersion: "2025-02-24.acacia"` (matches the SDK's `LatestApiVersion` type literal, recorded in a comment), `typescript: true` flag on the constructor, local `requireEnv(name)` that throws descriptive errors on missing env, `STRIPE_PRICE_IDS = { monthly: () => requireEnv("STRIPE_PRICE_ID_MONTHLY"), yearly: () => requireEnv("STRIPE_PRICE_ID_YEARLY") } as const` + `BillingPeriod = keyof typeof STRIPE_PRICE_IDS` type so Phase 2 actions are type-safe; new `src/lib/billing.ts` — `import "server-only"`, exports discriminated `CapacityCheck = { ok: true } | { ok: false; reason: string }`, `getUserIsPro(userId)` via dynamic `await import("@/lib/prisma")` (edge-runtime safety; `select: { isPro: true }` only, returns `false` when user missing) + the rule that write paths MUST use this instead of `session.user.isPro` since the JWT can carry a stale value across a Stripe downgrade, `checkItemCapacity(userId, isPro)` + `checkCollectionCapacity(userId, isPro)` that short-circuit `{ ok: true }` for Pro and otherwise compare `getXxxStatsForUser(userId).total` against the limit with user-facing reason strings (`"Free plan is limited to 50 items. Upgrade to Pro for unlimited."` / `"… 3 collections …"`); **deviation from spec** — `FREE_TIER_LIMITS = { items: 50, collections: 3 } as const` and `PRO_ONLY_ITEM_TYPES = new Set<string>(["file", "image"])` moved into `src/lib/constants.ts` (sitting alongside `ITEMS_PER_PAGE`, `FAVORITES_ITEMS_LIMIT`, `SYSTEM_TYPE_COLORS`, etc.) rather than colocated in `billing.ts` per user feedback during implementation that all shared/tunable constants should live in one centralized file — billing.ts imports them; NextAuth threading — `src/auth.config.ts` `jwt` callback now `async`, dynamically `await import("@/lib/prisma")` to `SELECT isPro` whenever `token.id` is set and writes `token.isPro = dbUser?.isPro ?? false` so a webhook upgrade/downgrade reflects on the user's next request without forcing a sign-out (the dynamic import keeps Prisma out of the edge bundle since the same config is consumed by middleware via `src/proxy.ts`); `session` callback unconditionally copies `token.isPro ?? false` onto `session.user.isPro` so consumers can rely on the boolean always being defined; `next build` confirmed the dynamic-import escape hatch DID work — middleware compiled clean and the edge bundle did not try to eagerly resolve `@/lib/prisma`, so the spec's fallback (moving the `jwt` callback into `src/auth.ts`) was not needed; `src/types/next-auth.d.ts` augments `Session.user` with `isPro: boolean` AND `@auth/core/jwt`'s `JWT` interface with `isPro: boolean` (NOT `next-auth/jwt` — re-export augmentation doesn't flow through, same gotcha as the existing `id` augmentation, with the existing comment in the file explaining why); `.env.example` cleanup — removed `STRIPE_PUBLISHABLE_KEY` (server-only is useless without the `NEXT_PUBLIC_` prefix; non-prefixed env vars are replaced with `""` in the client bundle per Next.js docs, and our Phase 2 Checkout flow is a server-side redirect that never ships Stripe.js to the browser), added a commented-out `# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=` placeholder with a two-line comment explaining when it would actually be needed (embedded Stripe Elements); the spec also called out a `STRIPE_PRICE__ID_YEARLY` double-underscore typo, but **the typo wasn't actually present** in the working-tree file — only the publishable-key removal + commented placeholder landed; also added `!.env.example` exception to `.gitignore` so the file can be tracked despite the blanket `.env*` ignore (the file itself was previously untracked); new Vitest coverage in `src/lib/__tests__/billing.test.ts` — 11 cases across three describe blocks: `getUserIsPro` (DB returns `isPro: true`, DB returns `isPro: false`, user missing returns `false` — verifies exact `where: { id }` + `select: { isPro: true }` shape on the first), `checkItemCapacity` (Pro user `{ ok: true }` and stats helper NOT called, Free under limit `{ ok: true }`, Free exactly at limit `{ ok: false }` with the exact reason string, Free over limit `ok === false`), `checkCollectionCapacity` (same 4-case matrix); mocks set up via `vi.mock("@/lib/prisma", ...)` + `vi.mock("@/lib/db/items", ...)` + `vi.mock("@/lib/db/collections", ...)` at the module boundary per the existing test pattern, so the dynamic `await import("@/lib/prisma")` inside `getUserIsPro` is still intercepted; per `coding-standards.md` Testing section, `src/lib/stripe.ts` is **not** unit-tested — it's a thin SDK wrapper whose behavior is "construct + cache + read env", deferred to Phase 2 where billing actions exercise it via a mocked `getStripe()`; 280 → 291 tests + `npm run build` pass; bundled into the commit: the Stripe planning corpus that drove this work — `docs/stripe-integration-plan.md` (the broader plan), `context/features/stripe-phase-1-spec.md` (this slice), `context/features/stripe-phase-2-spec.md` (next slice), `context/research/stripe-integration-research.md` (research notes) — all of which were previously sitting untracked in the working tree; **out of scope and deferred to Phase 2**: webhook handler (`/api/stripe/webhook`), checkout/portal server actions, settings billing UI + per-period CTAs, feature-gating in `createItemAction` / `createCollectionAction` / `/api/upload` via the new capacity helpers, cancel-on-delete subscription cleanup in `deleteAccountAction`, sidebar PRO badge wired to real `isPro` (currently still always-shown), marketing CTA → checkout flow, upgrade-prompt dialog, and the Stripe Dashboard product/price/webhook configuration itself — Completed
